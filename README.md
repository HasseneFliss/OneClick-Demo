# OneClick Demo App

A simple authentication and dashboard API.

## Installation
```bash
npm install
npm start
```

## API Endpoints

### POST /api/auth/register
Register a new user.

### POST /api/auth/login
Login and get JWT token.

### GET /api/dashboard
Get dashboard data (requires auth).